# Optional: Jev (TypeSafe System One) as a judge tier. Without it the judge is rule -> person,
# and GLiNER2.5-Decide (local, no key) still answers every case in shadow.
TYPESAFE_API_KEY=
# Optional: only for `duckbatch publish`.
HF_TOKEN=
